// TODO (Faza 5: lista tiketa + KB intercept prije kreiranja) — vidi TASKS.md
export default function TicketsPage() {
  return (
    <div className="p-8">
      <h1 className="text-xl font-semibold">TicketsPage</h1>
      <p className="text-muted-foreground">Placeholder — implementira se prema TASKS.md.</p>
    </div>
  );
}
